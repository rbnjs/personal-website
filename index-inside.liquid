---
layout: layout-inside.liquid
title: Serradas.org
description: Welcome to the English version of Ruben Serradas' personal website. Discover articles, projects, and insights shared by Ruben in a friendly and engaging manner.
keywords: Ruben Serradas, personal website, multilingual, projects, interests, blog
---

<h1>Welcome</h1>

<p>Hello friend, this is my website.</p>
<p>Hope you have a good time here.</p>

<section>
    <h2>Artículos</h2>
    <p>Artículos en Español:</p>

    {% assign latestArticles = collections.articulos | sort: 'date' | reverse %}
    <ul>
        {%- for article in latestArticles limit: 2 -%}
        <li><a href="{{ article.url | url }}">{{ article.data.title }}</a></li>
        {%- endfor -%}
    </ul>
</section>

<section>
    <h2>Articles</h2>
    <p>Some articles I've written:</p>

    {% assign latestArticles = collections.articles | sort: 'date' | reverse %}
    <ul>
        {%- for article in latestArticles limit: 2 -%}
        <li><a href="{{ article.url | url }}">{{ article.data.title }}</a></li>
        {%- endfor -%}
    </ul>
</section>

